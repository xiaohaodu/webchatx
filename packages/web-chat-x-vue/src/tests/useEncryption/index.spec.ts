// import useEncryption, { isValidUserId } from "../../../utils/index";

// const { id, peerId } = await useEncryption();
// console.log(id.value, peerId.value);
// console.log(isValidUserId(id.value));
